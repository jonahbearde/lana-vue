{
  "name": "lana",
  "version": "0.0.0",
  "private": true,
  "description": "A component library for Vue 3",
  "author": "jonabirde",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "build:lib": "pnpm --filter \"lana-vue\" run build"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^3.12.1",
    "eslint": "^9.17.0"
  }
}
